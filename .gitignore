# Build output
[Bb]in/
[Oo]bj/
.vs/

# User-specific files
*.user
*.suo

# Local database (created automatically on first run — don't commit real data)
*.db
*.db-shm
*.db-wal

# Real secrets — never commit this. Copy appsettings.example.json to appsettings.json
# and fill in your own values instead (see README.md).
OnlineComputerStore.Core/appsettings.json
